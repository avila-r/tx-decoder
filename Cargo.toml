[package]
name = "tx-decoder"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.217", features = ["derive"] }
dotenv = "0.15.0"
hex = "0.4.3"
serde_json = "1.0.135"

[features]
default = ["std"]
std = ["alloc"]
alloc = []
